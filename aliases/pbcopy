#!/usr/bin/env nash

#
# pbcopy alias
#

fn pbcopy(filename) {
	var current_dir, status <= pwd

	if $status != "0" {
		return
	}

	var file = $current_dir+"/"+$filename

	cat $file | xsel --clipboard --input
}

bindfn pbcopy pbcopy
